console.log("📄", "inference.ts");

(function () {
	const a = 1; // 1
	const b = 2; // 2
	const c = a + b; // 3

	console.log(a, typeof a); // number
	console.log(b, typeof b); // number
	console.log(c, typeof c); // number
})();
